const venue = {
  "id": 1,
  "name": "Store Washington",
  "description": "A wonderful Washington store",
  "phone": "123-456-7890",
  "venueLayout": {
    size:{
      width: 16.00, // Decimal(2)
      height: 16.00, // Decimal(2)
    },
    items: [
      {
        name: "string",
        type: "string",
        active: false,
        style:{
          heightRatio: 1, // Decimal(2)
          widthRatio: 1, // Decimal(2)
          size: 5, // Decimal(2)
          top: 50, // Decimal(2)
          left: 50, // Decimal(2)
          shape: "string",
        },
      },
    ],
  }
}