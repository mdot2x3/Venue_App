import React from 'react';
import './VenueFeed.scss';


const VenueFeed = ({props}) => {

  const render = () => {
    return (
      <div className="venue-feed">
        venue feed
      </div>
    );
  }

  return render();
};

export default VenueFeed;